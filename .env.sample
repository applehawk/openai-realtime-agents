# OpenAI API Key (required)
OPENAI_API_KEY=your_api_key

# Perplexity API Key (optional, for web research capabilities)
# Get your API key from: https://www.perplexity.ai/settings/api
PERPLEXITY_API_KEY=your_perplexity_api_key

# Auth API URL (used by the application)
NEXT_PUBLIC_AUTH_API_URL=https://rndaibot.ru/apib/v1

# CORS Configuration
# Leave empty for local development (localhost:3000) - no CORS headers will be added
# For production/cross-origin requests, set to comma-separated list of allowed origins
# Example: ALLOWED_ORIGINS=https://rndaibot.ru,https://app.example.com
ALLOWED_ORIGINS=

# Next.js configuration
PORT=3000

# Note: SSL certificate verification is automatically disabled in development mode
# for server-side fetch calls. This allows connecting to APIs with self-signed certificates
# during local development. In production, valid SSL certificates are required.